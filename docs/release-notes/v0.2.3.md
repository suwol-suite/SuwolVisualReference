# Suwol Visual Reference v0.2.3

This patch fixes the release asset policy so Windows is the only ZIP distribution target.

## Fixed

- Removed Linux ZIP from required release artifacts and release publication.
- Removed Linux ZIP from release asset verification, checksum required entries, and ZIP verification.
- Removed macOS ZIP from the manual macOS diagnostics and release attachment workflows.
- Kept Linux release output focused on AppImage and `latest-linux.yml`.
- Kept macOS release attachment focused on signed and notarized arm64 DMG plus optional update metadata.

## Release Assets

Expected core release assets:

- `SuwolVisualReference-0.2.3-win-x64.zip`
- `SuwolVisualReference-0.2.3-linux-x64.AppImage`
- `latest-linux.yml`
- `checksums.txt`
- `checksums.txt.asc`
- `SuwolVisualReference-0.2.3-checksums.txt`
- `SuwolVisualReference-0.2.3-checksums.txt.asc`
- `suwol-release-public-key.asc`
